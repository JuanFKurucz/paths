'use strict';

/*
  Clase no digna de explicacion
*/

export default class Coordinate {
  constructor(x,y){
    this.x=x;
    this.y=y;
  }

  //Copia una coordenada (?) a su propio valor (?)
  copy(cord){
    this.x=cord.x;
    this.y=cord.y;
  }
}
